// live-server HTTPS options (used via: live-server --https=./live-server-https.cjs)
// Cert generated by mkcert; trusted by your machine + any device that imports rootCA.pem.
const fs = require('node:fs');
const path = require('node:path');

const certDir = path.join(__dirname, '.certs');

module.exports = {
    cert: fs.readFileSync(path.join(certDir, 'dev.pem')),
    key:  fs.readFileSync(path.join(certDir, 'dev-key.pem'))
};
